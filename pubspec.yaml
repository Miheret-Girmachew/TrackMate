name: taskbudgettracker
description: A new Flutter project.

publish_to: 'none'  # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"  # Ensure Flutter version is compatible

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2               # HTTP package for making HTTP requests
  firebase_core: ^3.9.0      # Updated to latest version for Firebase initialization
  firebase_auth: ^5.3.4      # Updated to latest version for authentication
  firebase_auth_web: ^5.13.5  
  firebase_storage: ^12.3.7  # Updated firebase_storage version to resolve compatibility
  firebase_storage_web: ^3.10.6
  cloud_firestore: ^5.6.0    # Updated to match Firebase version compatibility
  google_sign_in: ^6.0.0
  uuid: ^3.0.7 
  shared_preferences: ^2.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0      # Ensure flutter_lints version is compatible

flutter:
  uses-material-design: true

  assets:
    - assets/afri.jpg         # Add your assets here
